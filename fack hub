local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Fack HUB",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "The Mimic",
   LoadingSubtitle = "by KENG",
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local aTab = Window:CreateTab("b1c1", "rewind")

local allowedPlaceIds = {6296321810}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = aTab:CreateSection("Auto Win")

local Button = aTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(3478.38696, 44.8275261, -1538.88599, 0, -1, 0, 1, 0, -0, 0, 0, 1)
   end,
})

local Section = aTab:CreateSection("ESP")

local Toggle = aTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace.Givers:GetChildren()) do
             if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
                 if v:FindFirstChild("Handle") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v.Handle
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
         end
      else
         for i, v in pairs(workspace.Givers:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})

local Toggle = aTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local allowedPlaceIds = {6301638949}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = aTab:CreateSection("Auto win")

local Button = aTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(1272.8075, 201.615051, -2537.38843, 0, 0, 1, 0, 1, -0, -1, 0, 0)
   end,
})

local Section = aTab:CreateSection("ESP")

local Toggle = aTab:CreateToggle({
   Name = "ESP Exit",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace.GroupTP:GetChildren()) do
             if v:IsA("Part") and not v:FindFirstChild("Fack HUB") then
                 if v.Name == "Touch2" then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = "Exit"
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
         end
      else
         for i, v in pairs(workspace.GroupTP:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})

local Toggle = aTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
		for k, v in pairs(workspace.GameAI2:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
	  	for k, v in pairs(workspace.GameAI2:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            	end
        	end
		end
   end,
})
end

local bTab = Window:CreateTab("b1c2", "rewind")

local allowedPlaceIds = {6373539583}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = bTab:CreateSection("Auto Win")

local Button = bTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(68.3324432, 61.9521561, -1614.55261, 0, 0, 1, 1, 0, 0, 0, 1, 0)
   end,
})

local Section = bTab:CreateSection("ESP")

local Toggle = bTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Key1" and not v:FindFirstChild("Fack HUB") then
                 local handle = v:FindFirstChild("Handle")
                 if handle then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = handle
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end

         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Butterfly" and not v:FindFirstChild("Fack HUB") then
                 local part = v.PrimaryPart or v:FindFirstChildWhichIsA("BasePart")
                 if part then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = part
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end
      else
         local keyModel = workspace:FindFirstChild("Key1")
         if keyModel then
            for i, v in pairs(keyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end

         local butterflyModel = workspace:FindFirstChild("Butterfly")
         if butterflyModel then
            for i, v in pairs(butterflyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end
      end
   end,
})


local Toggle = bTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local allowedPlaceIds = {6406571212}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = bTab:CreateSection("Auto Win")

local Button = bTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(232.938049, 101.941238, -585.303162, 0.999673665, 3.42568853e-08, -0.0255460832, -3.430997e-08, 1, -1.63968195e-09, 0.0255460832, 2.51563215e-09, 0.999673665)
   end,
})

local Section = bTab:CreateSection("ESP")

local Toggle = bTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Key1" and not v:FindFirstChild("Fack HUB") then
                 local handle = v:FindFirstChild("Handle")
                 if handle then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = handle
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end

         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Butterfly" and not v:FindFirstChild("Fack HUB") then
                 local part = v.PrimaryPart or v:FindFirstChildWhichIsA("BasePart")
                 if part then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = part
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end
      else
         local keyModel = workspace:FindFirstChild("Key1")
         if keyModel then
            for i, v in pairs(keyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end

         local butterflyModel = workspace:FindFirstChild("Butterfly")
         if butterflyModel then
            for i, v in pairs(butterflyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end
      end
   end,
})

local Toggle = bTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
		for k, v in pairs(workspace.GameAI2:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
	  	for k, v in pairs(workspace.GameAI2:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            	end
        	end
		end
   end,
})
end

local allowedPlaceIds = {6425178683}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = bTab:CreateSection("Auto Win")

local Button = bTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(863.558289, 79.4404068, -353.719604, -1.1920929e-07, 0, 1.00000012, 0, 1, 0, -1.00000012, 0, -1.1920929e-07)
   end,
})

local Section = bTab:CreateSection("ESP")

local Toggle = bTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Key1" and not v:FindFirstChild("Fack HUB") then
                 local handle = v:FindFirstChild("Handle")
                 if handle then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = handle
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end

         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Butterfly" and not v:FindFirstChild("Fack HUB") then
                 local part = v.PrimaryPart or v:FindFirstChildWhichIsA("BasePart")
                 if part then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = part
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end
      else
         local keyModel = workspace:FindFirstChild("Key1")
         if keyModel then
            for i, v in pairs(keyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end

         local butterflyModel = workspace:FindFirstChild("Butterfly")
         if butterflyModel then
            for i, v in pairs(butterflyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end
      end
   end,
})

local Toggle = bTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local cTab = Window:CreateTab("b1c3", "rewind")

local allowedPlaceIds = {6472459099}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = cTab:CreateSection("Auto Win")

local Button = cTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(2411.87427, -15.7511759, 2285.42798, 0, 0, 1, 1, 0, 0, 0, 1, 0)
   end,
})

local Section = cTab:CreateSection("ESP")

local Toggle = cTab:CreateToggle({
    Name = "ESP item",
    CurrentValue = false,
    Flag = "Toggle1",
    Callback = function(Value)
        if Value then
            for i, v in pairs(workspace:GetChildren()) do
                if v.Name == "Crowbar" and not v:FindFirstChild("Fack HUB") then
                    local handle = v:FindFirstChild("Handle")
                    if handle then
                        local BillboardGui = Instance.new("BillboardGui")
                        BillboardGui.Parent = handle
                        BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                        BillboardGui.Active = true
                        BillboardGui.Name = "Fack HUB"
                        BillboardGui.AlwaysOnTop = true
                        BillboardGui.LightInfluence = 1.000
                        BillboardGui.Size = UDim2.new(0, 200, 0, 200)

                        local TextLabel = Instance.new("TextLabel")
                        TextLabel.Parent = BillboardGui
                        TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                        TextLabel.BackgroundTransparency = 1.000
                        TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                        TextLabel.BorderSizePixel = 0
                        TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                        TextLabel.Size = UDim2.new(0, 200, 0, 50)
                        TextLabel.Font = Enum.Font.FredokaOne
                        TextLabel.Text = v.Name
                        TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                        TextLabel.TextSize = 25
                        TextLabel.TextStrokeTransparency = 0
                        TextLabel.TextWrapped = true
                    end
                end
            end
			for i, v in pairs(workspace:GetChildren()) do
                if v.Name == "Blade" and not v:FindFirstChild("Fack HUB") then
                    local handle = v:FindFirstChild("Handle")
                    if handle then
                        local BillboardGui = Instance.new("BillboardGui")
                        BillboardGui.Parent = handle
                        BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                        BillboardGui.Active = true
                        BillboardGui.Name = "Fack HUB"
                        BillboardGui.AlwaysOnTop = true
                        BillboardGui.LightInfluence = 1.000
                        BillboardGui.Size = UDim2.new(0, 200, 0, 200)

                        local TextLabel = Instance.new("TextLabel")
                        TextLabel.Parent = BillboardGui
                        TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                        TextLabel.BackgroundTransparency = 1.000
                        TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                        TextLabel.BorderSizePixel = 0
                        TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                        TextLabel.Size = UDim2.new(0, 200, 0, 50)
                        TextLabel.Font = Enum.Font.FredokaOne
                        TextLabel.Text = v.Name
                        TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                        TextLabel.TextSize = 25
                        TextLabel.TextStrokeTransparency = 0
                        TextLabel.TextWrapped = true
                    end
                end
            end
			for i, v in pairs(workspace:GetChildren()) do
                if v:IsA("Model") and v.Name == "Butterfly" and not v:FindFirstChild("Fack HUB") then
                        local BillboardGui = Instance.new("BillboardGui")
                        BillboardGui.Parent = v
                        BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                        BillboardGui.Active = true
                        BillboardGui.Name = "Fack HUB"
                        BillboardGui.AlwaysOnTop = true
                        BillboardGui.LightInfluence = 1.000
                        BillboardGui.Size = UDim2.new(0, 200, 0, 200)

                        local TextLabel = Instance.new("TextLabel")
                        TextLabel.Parent = BillboardGui
                        TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                        TextLabel.BackgroundTransparency = 1.000
                        TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                        TextLabel.BorderSizePixel = 0
                        TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                        TextLabel.Size = UDim2.new(0, 200, 0, 50)
                        TextLabel.Font = Enum.Font.FredokaOne
                        TextLabel.Text = v.Name
                        TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                        TextLabel.TextSize = 25
                        TextLabel.TextStrokeTransparency = 0
                        TextLabel.TextWrapped = true
                    end
                end
        else
            -- ทำลาย BillboardGui ทุกตัวที่ชื่อ "Fack HUB" ใน Crowbar ทุกตัว
            for i, v in pairs(workspace:GetChildren()) do
                if v.Name == "Crowbar" then
                    for i, v in pairs(v:GetDescendants()) do
                        if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                            v:Destroy()
                        end
                    end
                end
            end
		for i, v in pairs(workspace:GetChildren()) do
                if v.Name == "Blade" then
                    for i, v in pairs(v:GetDescendants()) do
                        if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                            v:Destroy()
                        end
                    end
                end
            end
			for i, v in pairs(workspace:GetChildren()) do
                if v.Name == "Butterfly" then
                    for i, v in pairs(v:GetDescendants()) do
                        if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                            v:Destroy()
                        end
                    end
                end
            end
        end
    end,
})

local Toggle = cTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local allowedPlaceIds = {6682163754}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = cTab:CreateSection("Auto Win")

local Button = cTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(242.771713, 34.3152275, 449.323639, 0, 0, 1, 1, 0, 0, 0, 1, 0)
   end,
})

local Section = cTab:CreateSection("ESP")

local Toggle = cTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
   if Value then
		for i, v in pairs(workspace.Givers:GetDescendants()) do
             if v:FindFirstChild("Handle") and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
		for i, v in pairs(workspace.Rose:GetDescendants()) do
             if v:IsA("MeshPart") and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = "Rose"
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
		for i, v in pairs(workspace.BellFolder:GetDescendants()) do
             if v:IsA("MeshPart") and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = "Bell"
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
     else
         for i, v in pairs(workspace.Givers:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
        end
		for i, v in pairs(workspace.Rose:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
        end
	    for i, v in pairs(workspace.BellFolder:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
        end
	end
   end,
})

local Toggle = cTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and v.Name == "AI" and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local allowedPlaceIds = {6682164423}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = cTab:CreateSection("Auto Win")

local Button = cTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(-643.695007, 948.410156, -1491.67163, 0, 0, 1, 0, 1, -0, -1, 0, 0)
   end,
})

local Section = cTab:CreateSection("ESP")

local Toggle = cTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace:GetChildren()) do
             if v:IsA("Model") and v.Name == "Key1" and not v:FindFirstChild("Fack HUB") then
                 local handle = v:FindFirstChild("Handle")
                 if handle then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = handle
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 0
                     BillboardGui.Size = UDim2.new(0, 100, 0, 50)
                     BillboardGui.StudsOffset = Vector3.new(0, 2, 0)

                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundTransparency = 1
                     TextLabel.Size = UDim2.new(1, 0, 1, 0)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = "Key"
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 20
                     TextLabel.TextStrokeTransparency = 0
                 end
             end
         end
for k, v in ipairs(workspace:GetChildren()) do
    if v:IsA("Model") and v.Name == "CodeDoor" and not v:FindFirstChild("Fack HUB") then
		for i, v in ipairs(v:GetChildren()) do
			if v:IsA("MeshPart") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(0, 255, 0) 
               highlight.OutlineColor = Color3.fromRGB(0, 255, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
        end
	end
end
      else
         local keyModel = workspace:FindFirstChild("Key1")
         if keyModel then
            for i, v in pairs(keyModel:GetDescendants()) do
                if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
                    v:Destroy()
                end
            end
         end
	for i, v in ipairs(workspace:GetChildren()) do
        if v:IsA("Model") and v.Name == "CodeDoor" then
			for i, v in ipairs(v:GetDescendants()) do
				if v:IsA("Highlight") and v.Name == "Fack HUB" then
              		v:Destroy()
            end
         end
      end
	end
end
   end,
})

local Toggle = cTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace:GetChildren()) do
            if v:IsA("Model") and v.Name == "omukadeMAIN" and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.omukadeMAIN:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local dTab = Window:CreateTab("b1c4", "rewind")

local allowedPlaceIds = {7251865082}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = dTab:CreateSection("Auto Win")

local Button = dTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
      game.Players.LocalPlayer.character.HumanoidRootPart.CFrame = CFrame.new(88.1409454, -47.2177963, -1415.82458, 4.47034836e-06, -0.00181728601, -0.99999845, 1, 4.47034836e-06, 4.55975533e-06, 4.55975533e-06, -0.99999845, 0.00181722641)
   end,
})

local Section = dTab:CreateSection("ESP")

local Toggle = dTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
local children = workspace:GetChildren()

if #children >= 120 then
    local model = children[120]
    if model:FindFirstChild("Model") and model.Model:FindFirstChild("Model") then
        for i, v in pairs(model.Model.Model:GetDescendants()) do
            if v:FindFirstChild("Handle") and not v:FindFirstChild("Fack HUB") then
                local BillboardGui = Instance.new("BillboardGui")
                BillboardGui.Parent = v
                BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                BillboardGui.Active = true
                BillboardGui.Name = "Fack HUB"
                BillboardGui.AlwaysOnTop = true
                BillboardGui.LightInfluence = 1.000
                BillboardGui.Size = UDim2.new(0, 200, 0, 200)
                
                local TextLabel = Instance.new("TextLabel")
                TextLabel.Parent = BillboardGui
                TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                TextLabel.BackgroundTransparency = 1.000
                TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                TextLabel.BorderSizePixel = 0
                TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                TextLabel.Size = UDim2.new(0, 200, 0, 50)
                TextLabel.Font = Enum.Font.FredokaOne
                TextLabel.Text = "Key1"
                TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                TextLabel.TextSize = 25
                TextLabel.TextStrokeTransparency = 0
                TextLabel.TextWrapped = true
            end
        end
    end
end
local children = workspace:GetChildren()
local targetChild = children[31]

if targetChild and targetChild:IsA("Model") and targetChild:FindFirstChild("Handle") and not targetChild.Handle:FindFirstChild("Fack HUB") then
    local BillboardGui = Instance.new("BillboardGui")
    BillboardGui.Parent = targetChild.Handle
    BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    BillboardGui.Active = true
    BillboardGui.Name = "Fack HUB"
    BillboardGui.AlwaysOnTop = true
    BillboardGui.LightInfluence = 1.000
    BillboardGui.Size = UDim2.new(0, 200, 0, 200)
    
    local TextLabel = Instance.new("TextLabel")
    TextLabel.Parent = BillboardGui
    TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.BackgroundTransparency = 1.000
    TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
    TextLabel.Size = UDim2.new(0, 200, 0, 50)
    TextLabel.Font = Enum.Font.FredokaOne
    TextLabel.Text = "Key2"
    TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
    TextLabel.TextSize = 25
    TextLabel.TextStrokeTransparency = 0
    TextLabel.TextWrapped = true
end
local children = workspace:GetChildren()
local targetChild = children[29]

if targetChild and targetChild:IsA("Model") and targetChild:FindFirstChild("Handle") and not targetChild.Handle:FindFirstChild("Fack HUB") then
    local BillboardGui = Instance.new("BillboardGui")
    BillboardGui.Parent = targetChild.Handle
    BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    BillboardGui.Active = true
    BillboardGui.Name = "Fack HUB"
    BillboardGui.AlwaysOnTop = true
    BillboardGui.LightInfluence = 1.000
    BillboardGui.Size = UDim2.new(0, 200, 0, 200)
    
    local TextLabel = Instance.new("TextLabel")
    TextLabel.Parent = BillboardGui
    TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.BackgroundTransparency = 1.000
    TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
    TextLabel.Size = UDim2.new(0, 200, 0, 50)
    TextLabel.Font = Enum.Font.FredokaOne
    TextLabel.Text = "Crowbars"
    TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
    TextLabel.TextSize = 25
    TextLabel.TextStrokeTransparency = 0
    TextLabel.TextWrapped = true
end
local children = workspace:GetChildren()
local targetChild = children[27]

if targetChild and targetChild:IsA("Model") and targetChild:FindFirstChild("Handle") and not targetChild.Handle:FindFirstChild("Fack HUB") then
    local BillboardGui = Instance.new("BillboardGui")
    BillboardGui.Parent = targetChild.Handle
    BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
    BillboardGui.Active = true
    BillboardGui.Name = "Fack HUB"
    BillboardGui.AlwaysOnTop = true
    BillboardGui.LightInfluence = 1.000
    BillboardGui.Size = UDim2.new(0, 200, 0, 200)
    
    local TextLabel = Instance.new("TextLabel")
    TextLabel.Parent = BillboardGui
    TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.BackgroundTransparency = 1.000
    TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
    TextLabel.Size = UDim2.new(0, 200, 0, 50)
    TextLabel.Font = Enum.Font.FredokaOne
    TextLabel.Text = "Key3"
    TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
    TextLabel.TextSize = 25
    TextLabel.TextStrokeTransparency = 0
    TextLabel.TextWrapped = true
end
	else
         for i, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})

local Toggle = dTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local allowedPlaceIds = {7251866503}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = dTab:CreateSection("Auto Win")

local Button = dTab:CreateButton({
   Name = "Auto Win",
   Callback = function()
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(1025.33289, 2.67823577, 122.2911, 1, 0, 0, 0, 1, 0, 0, 0, 1)
wait(0.5)
   local butterflies = game.Workspace.Butterflies:GetChildren()

for i, v in ipairs(butterflies) do
    for i, v in ipairs(v:GetChildren()) do
        if v:IsA("ProximityPrompt") then
            v.HoldDuration = 0
            v.MaxActivationDistance = 10
            v.RequiresLineOfSight = false
            v.Exclusivity = Enum.ProximityPromptExclusivity.AlwaysShow
        end
    end
end

local camera = workspace.CurrentCamera
local desiredCFrame = CFrame.new(Vector3.new(0, 10, 0), Vector3.new(0, 0, 0))
local prl = game.Players.LocalPlayer.Character.HumanoidRootPart

for i, v in pairs(workspace.Butterflies:GetChildren()) do
	if v.Name == "Butterfly" then
		local prox = v:FindFirstChild("ProximityPrompt")
		prl.CFrame = v.CFrame
		camera.CFrame = desiredCFrame
		if prox then
		wait(0.2)
		fireproximityprompt(prox)
		wait(0.2)
		end
	end
end
   end,
})

local Section = dTab:CreateSection("ESP")

local Toggle = dTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
      if Value then
         for i, v in pairs(workspace.Butterflies:GetChildren()) do
             if v.Name == "Butterfly" and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
		 for k, v in pairs(workspace.Butterflies:GetChildren()) do
            if v:IsA("Part") and not v:IsA("Highlight") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(0, 255, 0) 
               highlight.OutlineColor = Color3.fromRGB(0, 255, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for i, v in pairs(workspace.Butterflies:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
         for k, v in pairs(workspace.Butterflies:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})

local Toggle = dTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end

local allowedPlaceIds = {7251867155}

if table.find(allowedPlaceIds, game.PlaceId) then

local Section = dTab:CreateSection("Auto Win")



local Section = dTab:CreateSection("ESP")

local Toggle = dTab:CreateToggle({
   Name = "ESP item",
   CurrentValue = false,
   Flag = "Toggle1",
   Callback = function(Value)
   if Value then
	         for i, v in pairs(workspace:GetChildren()) do
             if v.Name == "ThreeDTextObject" then
			 for i, v in pairs(v:GetChildren()) do
				 if v.Name == "ThreeDTextBoundingBox" and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = 2573
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
		end
	end
         for i, v in pairs(workspace:GetDescendants()) do
             if v.Name == "Spirit" and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = "Butterfly"
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
         for i, v in pairs(workspace:GetChildren()) do
             if v.Name == "Key" then
			 	for i, v in pairs(v:GetChildren()) do
				 if v.Name == "Handle" and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = "Key"
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
		end
	end
         for i, v in pairs(workspace.GameInfo.PuzzleItems:GetChildren()) do
             if v.Name == "GrabItem" and not v:FindFirstChild("Fack HUB") then
                     local BillboardGui = Instance.new("BillboardGui")
                     BillboardGui.Parent = v
                     BillboardGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
                     BillboardGui.Active = true
                     BillboardGui.Name = "Fack HUB"
                     BillboardGui.AlwaysOnTop = true
                     BillboardGui.LightInfluence = 1.000
                     BillboardGui.Size = UDim2.new(0, 200, 0, 200)
     
                     local TextLabel = Instance.new("TextLabel")
                     TextLabel.Parent = BillboardGui
                     TextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
                     TextLabel.BackgroundTransparency = 1.000
                     TextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
                     TextLabel.BorderSizePixel = 0
                     TextLabel.Position = UDim2.new(0, 0, 0.2, 0)
                     TextLabel.Size = UDim2.new(0, 200, 0, 50)
                     TextLabel.Font = Enum.Font.FredokaOne
                     TextLabel.Text = v.Name
                     TextLabel.TextColor3 = Color3.fromRGB(81, 255, 0)
                     TextLabel.TextSize = 25
                     TextLabel.TextStrokeTransparency = 0
                     TextLabel.TextWrapped = true
                 end
             end
      else
         for i, v in pairs(workspace:GetDescendants()) do
            if v:IsA("BillboardGui") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
	end
   end,
})

local Toggle = dTab:CreateToggle({
   Name = "ESP Monster",
   CurrentValue = false,
   Flag = "Toggle1", 
   Callback = function(Value)
      if Value then
         for k, v in pairs(workspace.GameAI:GetChildren()) do
            if v:IsA("Model") and not v:FindFirstChild("Fack HUB") then
               local highlight = Instance.new("Highlight")
               highlight.Parent = v
               highlight.Name = "Fack HUB"
               highlight.FillColor = Color3.fromRGB(255, 0, 0) 
               highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
               highlight.FillTransparency = 0.5
               highlight.OutlineTransparency = 0
               highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            end
         end
      else
         for k, v in pairs(workspace.GameAI:GetDescendants()) do
            if v:IsA("Highlight") and v.Name == "Fack HUB" then
               v:Destroy()
            end
         end
      end
   end,
})
end
game:GetService("CoreGui")["Rayfield-Old"]:Destroy()
